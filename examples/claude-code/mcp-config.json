{
  "$comment": "Merge the anode entry into your MCP client's config and correct the path if anode.exe lives elsewhere. `timeout` (milliseconds) is read by Claude Code only; other clients ignore it, so start the seat with `anode start --hidden` before the first request. Claude Desktop: https://github.com/skulitom/Anode/blob/main/docs/CONNECTING-AGENTS.md#claude-desktop",
  "mcpServers": {
    "anode": {
      "command": "C:\\Users\\you\\AppData\\Local\\Programs\\Anode\\anode.exe",
      "args": ["mcp"],
      "timeout": 420000
    }
  }
}
